To deploy this app to the server, just zip all files and then unzip there. 

To run the app at the server (curator.reactome.org), use the shell script, run_llm.sh. To stop the app, kill the process after finding the process id.

